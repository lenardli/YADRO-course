apiVersion: v1
kind: Service
metadata:
  name: {{ include "yadro-currency-app.fullname" . }}
  labels:
    {{- include "yadro-currency-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "yadro-currency-app.selectorLabels" . | nindent 4 }}
