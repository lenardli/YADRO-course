---
argument_specs:
  main:
    short_description: Install kubeadm from pkgs.k8s.io and hold the package version.
    options:
      kubernetes_version:
        type: str
        description: Kubernetes apt repo channel under pkgs.k8s.io core/stable (e.g. v1.35).
        default: "v1.35"
